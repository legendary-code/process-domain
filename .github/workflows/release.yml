name: Release

on:
  release:
    types:
      - published

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
        name: Checkout

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.0.2

      - name: Setup NuGet
        uses: nuget/setup-nuget@v1
        with:
          nuget-api-key: ${{ secrets.NUGET_API_KEY }}

      - name: Install NuGet Packages
        run: nuget restore ProcessDomain.sln

      - name: Install MSBuildTasks Package
        run: nuget install MSBuildTasks -Version 1.5.0.235

      - name: Generate GlobalAssemblyInfo.cs
        run: msbuild .\Version.proj /p:Version=${{ github.ref }}

      - name: Build Release
        run: msbuild ProcessDomain.sln /property:Configuration=Release

      - name: Create NuGet Package
        run: nuget pack .\AppSecInc.ProcessDomain.nuspec -p version=${{ github.ref }}

      - name: Publish NuGet Package
        run: nuget push AppSecInc.ProcessDomain.${{ github.ref }}.nupkg -Source https://api.nuget.org/v3/index.json
